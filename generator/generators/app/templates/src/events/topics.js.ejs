/**
 * Consume
 */<% consumers.forEach(consumer => { %>
module.exports.<%= consumer.variableName %> = '<%= consumer.value %>';<% }) %>

/**
 * Produce
 */<% producers.forEach(producer => { %>
module.exports.<%= producer.variableName %> = '<%= producer.value %>';<% }) %>