version: "3"
services:
  app:
    build: .
    command: sh -c "yarn lint-check && yarn ${ENV:-dev}"
    volumes:
      - ./src:/app/src
      - /app/node_modules
    networks:
      - app
    logging:
      driver: ${LOGGING_DRIVER:-json-file}
    restart: always

networks:
  app:
    driver: bridge
